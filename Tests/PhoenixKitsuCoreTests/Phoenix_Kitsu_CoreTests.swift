import XCTest
@testable import Phoenix_Kitsu_Core

class Phoenix_Kitsu_CoreTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Phoenix_Kitsu_Core().text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
